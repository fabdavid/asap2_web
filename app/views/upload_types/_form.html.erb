<%= form_with(model: upload_type, local: true) do |form| %>
  <% if upload_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(upload_type.errors.count, "error") %> prohibited this upload_type from being saved:</h2>

      <ul>
      <% upload_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
