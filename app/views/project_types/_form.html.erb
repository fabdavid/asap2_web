<%= form_with(model: project_type, local: true) do |form| %>
  <% if project_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_type.errors.count, "error") %> prohibited this project_type from being saved:</h2>

      <ul>
      <% project_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class='form-group col-12 col-sm-4'>
    <%= label_tag 'name', 'Name', {'data-toggle' => "tooltip"} %>
    <%= form.text_field :name, {:class => "form-control", :placeholder => "Name of the project type"} %>
  </div>

  <div class='form-group col-12 col-sm-4'>
    <%= label_tag 'tag', 'Tag', {'data-toggle' => "tooltip"} %>
    <%= form.text_field :tag, {:class => "form-control", :placeholder => "Tag of the project type"} %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
