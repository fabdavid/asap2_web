<%= form_with(model: norm, local: true) do |form| %>
  <% if norm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(norm.errors.count, "error") %> prohibited this norm from being saved:</h2>

      <ul>
      <% norm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
