<%= form_with(model: geo_entry, local: true) do |form| %>
  <% if geo_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geo_entry.errors.count, "error") %> prohibited this geo_entry from being saved:</h2>

      <ul>
      <% geo_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
