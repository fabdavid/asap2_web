<%= form_with(model: ott_project, local: true) do |form| %>
  <% if ott_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ott_project.errors.count, "error") %> prohibited this ott_project from being saved:</h2>

      <ul>
      <% ott_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
