<%= form_with(model: del_run, local: true) do |form| %>
  <% if del_run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(del_run.errors.count, "error") %> prohibited this del_run from being saved:</h2>

      <ul>
      <% del_run.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
