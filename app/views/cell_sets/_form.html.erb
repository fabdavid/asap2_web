<%= form_with(model: cell_set, local: true) do |form| %>
  <% if cell_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cell_set.errors.count, "error") %> prohibited this cell_set from being saved:</h2>

      <ul>
      <% cell_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
