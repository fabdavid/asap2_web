<p id="notice"><%= notice %></p>

<h1>Cell Sets</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cell_sets.each do |cell_set| %>
      <tr>
        <td><%= link_to 'Show', cell_set %></td>
        <td><%= link_to 'Edit', edit_cell_set_path(cell_set) %></td>
        <td><%= link_to 'Destroy', cell_set, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cell Set', new_cell_set_path %>
