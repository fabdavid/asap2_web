<%= form_for(version) do |f| %>
  <% if version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(version.errors.count, "error") %> prohibited this version from being saved:</h2>

      <ul>
      <% version.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
<tr><td>Release date</td><td><%= f.date_select :release_date %></td></tr>
<tr><td>Description</td><td><%= f.text_area :description %></td></tr>
<tr><td>Tools</td><td><%= f.text_area :tools_json %></td></tr>
</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
