<%= form_with(model: std_run, local: true) do |form| %>
  <% if std_run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(std_run.errors.count, "error") %> prohibited this std_run from being saved:</h2>

      <ul>
      <% std_run.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
