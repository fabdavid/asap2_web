<%= form_with(model: archive_status, local: true) do |form| %>
  <% if archive_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(archive_status.errors.count, "error") %> prohibited this archive_status from being saved:</h2>

      <ul>
      <% archive_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
