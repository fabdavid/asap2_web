<p id="notice"><%= notice %></p>

<h1>Cell Filterings</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cell_filterings.each do |cell_filtering| %>
      <tr>
        <td><%= link_to 'Show', cell_filtering %></td>
        <td><%= link_to 'Edit', edit_cell_filtering_path(cell_filtering) %></td>
        <td><%= link_to 'Destroy', cell_filtering, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cell Filtering', new_cell_filtering_path %>
