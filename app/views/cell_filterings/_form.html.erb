<%= form_with(model: cell_filtering, local: true) do |form| %>
  <% if cell_filtering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cell_filtering.errors.count, "error") %> prohibited this cell_filtering from being saved:</h2>

      <ul>
      <% cell_filtering.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
