<%= form_with(model: tmp_fo, local: true) do |form| %>
  <% if tmp_fo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tmp_fo.errors.count, "error") %> prohibited this tmp_fo from being saved:</h2>

      <ul>
      <% tmp_fo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
