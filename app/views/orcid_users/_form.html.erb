<%= form_with(model: orcid_user, local: true) do |form| %>
  <% if orcid_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(orcid_user.errors.count, "error") %> prohibited this orcid_user from being saved:</h2>

      <ul>
      <% orcid_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
