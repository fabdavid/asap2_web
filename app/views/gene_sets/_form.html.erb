<%= form_with(model: gene_set, local: true) do |form| %>
  <% if gene_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gene_set.errors.count, "error") %> prohibited this gene_set from being saved:</h2>

      <ul>
      <% gene_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
