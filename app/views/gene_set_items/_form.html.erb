<%= form_with(model: gene_set_item, local: true) do |form| %>
  <% if gene_set_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gene_set_item.errors.count, "error") %> prohibited this gene_set_item from being saved:</h2>

      <ul>
      <% gene_set_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
