<%= form_with(model: data_type, local: true) do |form| %>
  <% if data_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(data_type.errors.count, "error") %> prohibited this data_type from being saved:</h2>

      <ul>
      <% data_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
