<%= form_with(model: gene_filtering, local: true) do |form| %>
  <% if gene_filtering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gene_filtering.errors.count, "error") %> prohibited this gene_filtering from being saved:</h2>

      <ul>
      <% gene_filtering.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
