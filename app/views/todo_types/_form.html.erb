<%= form_with(model: todo_type, local: true) do |form| %>
  <% if todo_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_type.errors.count, "error") %> prohibited this todo_type from being saved:</h2>

      <ul>
      <% todo_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
