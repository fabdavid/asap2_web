<%= form_with(model: covariate, local: true) do |form| %>
  <% if covariate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(covariate.errors.count, "error") %> prohibited this covariate from being saved:</h2>

      <ul>
      <% covariate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
