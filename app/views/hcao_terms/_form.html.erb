<%= form_with(model: hcao_term, local: true) do |form| %>
  <% if hcao_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hcao_term.errors.count, "error") %> prohibited this hcao_term from being saved:</h2>

      <ul>
      <% hcao_term.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
